% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/versebus.R
\name{vb_download}
\alias{vb_download}
\title{Verified, atomic release-asset download}
\usage{
vb_download(
  repo,
  tag,
  name,
  dest,
  manifest = NULL,
  require_manifest = isTRUE(Sys.getenv("VERSEBUS_STRICT") == "1"),
  max_age = NULL
)
}
\arguments{
\item{manifest}{pass a manifest to verify sha256; NULL fetches it
(legacy mode when the tag has none)}

\item{require_manifest}{TRUE in CI/strict mode (VERSEBUS_STRICT=1)}

\item{max_age}{optional difftime; manifest older than this raises
\code{vb_error_stale}}
}
\description{
Downloads to a tempfile in \code{dest}'s directory, verifies (parquet magic
bytes; sha256 vs manifest when available, size vs asset list otherwise),
then atomically renames into place and writes a \verb{<dest>.sha256} sidecar.
ON ANY FAILURE the temp is deleted and a pre-existing \code{dest} is left
untouched — but it is NEVER silently served as a fallback: the typed error
propagates and the caller must opt in to "serve stale + warn" explicitly.
}
\keyword{internal}
