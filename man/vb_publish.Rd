% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/versebus.R
\name{vb_publish}
\alias{vb_publish}
\title{Manifest-last atomic publish (the versebus producer pattern)}
\usage{
vb_publish(
  paths,
  repo,
  tag,
  rows = NULL,
  carry_forward = TRUE,
  min_row_frac = NULL,
  max_retries = 2,
  dry_run = FALSE
)
}
\arguments{
\item{paths}{character vector of local files to upload}

\item{rows}{optional named integer vector: rows per basename}

\item{carry_forward}{merge with the previous manifest so partial publishes
still describe the whole tag}

\item{min_row_frac}{optional floor vs previous manifest rows (e.g. 0.9)}

\item{dry_run}{build + return the manifest without uploading anything}
}
\description{
Ordered: hash → floor-check → upload data assets (bounded retries, collect
failures) → gate (any failure aborts BEFORE the manifest, so consumers
keep the last consistent snapshot) → verify live asset list → upload
bus_manifest.json LAST → fire the cache-invalidation hook
(\code{options(versebus.on_publish = function(repo, tag) ...)}).
}
\keyword{internal}
