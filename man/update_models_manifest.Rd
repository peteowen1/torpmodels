% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/publish.R
\name{update_models_manifest}
\alias{update_models_manifest}
\title{Read-modify-write the models_manifest.json ledger}
\usage{
update_models_manifest(files, dir, repo, tag)
}
\arguments{
\item{files}{Character vector of file names (relative to \code{dir}).}

\item{dir}{Character. Directory containing the files.}

\item{repo}{Character. \code{"owner/repo"}.}

\item{tag}{Character. Release tag.}
}
\value{
The updated manifest list, invisibly.
}
\description{
Downloads the current manifest (a 404 means "start fresh"; any other
download error aborts rather than risking a clobber), records a new
entry per file (sha256, size, timestamp, and a meta subset when the file
carries a \code{torp_meta} stamp), moves the previous entry for that artifact
onto its \code{history} (capped at 20), and re-uploads the manifest last so
artifacts always land before the ledger claims them.
}
