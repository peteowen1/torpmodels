% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_meta.R
\name{build_model_meta}
\alias{build_model_meta}
\title{Build model provenance metadata}
\usage{
build_model_meta(
  model_name,
  seasons,
  params,
  feature_names,
  cv_metric = NA_real_,
  n_rows = NA_integer_,
  n_matches = NA_integer_,
  extra = list()
)
}
\arguments{
\item{model_name}{Character. Short model id, e.g. "wp", "ep", "shot".}

\item{seasons}{Integer vector of training seasons, or a pre-formatted
"YYYY-YYYY" character range.}

\item{params}{List of hyperparameters used to train the model (includes
the monotone constraints string for WP).}

\item{feature_names}{Character vector of feature names in model order.}

\item{cv_metric}{Numeric. Cross-validated metric (logloss/mlogloss).}

\item{n_rows}{Integer. Number of training rows.}

\item{n_matches}{Integer. Number of training matches.}

\item{extra}{Named list merged into the returned meta. Supports a
\code{script} element naming the training script (recorded as the top-level
\code{script} field); any other elements are merged in as-is, e.g.
\code{ep_source}, \code{ep_artifact_sha256}.}
}
\value{
A named list with the required provenance fields.
}
\description{
Returns a list of provenance fields to be stamped onto a trained model
object via \code{\link[=stamp_model_meta]{stamp_model_meta()}}. This is the single source of truth for
what "we trained this model, here's how" means across every trainer.
}
\keyword{internal}
